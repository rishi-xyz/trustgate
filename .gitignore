# ignoring private building notes , status and other files
.agent
.agents
.claude
skills-lock.json
.env
*.pem
.trustgate-dev/
bin/
build/
# generated locally by `make publish` / demos
registry/manifests/
receipt.json

# outputs and secrets from running jobs (never commit)
recipient.key
*.salts.json
*.meta
sealed.json
conf*.json
blob.json
async.json
plain*.json
evil.json
receipt.json

# per-machine client config (contains endpoint addresses)
opencode.json
.mcp.json

# guided-demo output (contains keys, salts and receipts)
.demo/
brag-output

# SAM build output
.aws-sam/

# Python virtualenv for the Strands demo agent
agents/.venv/
__pycache__/

# release binaries built by `make dist`
dist/
